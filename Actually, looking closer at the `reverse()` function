        node *old_first = head->next;
        node *old_last = head->prev;
        head->next = old_last;
        head->prev = old_first;
        head->next->prev = head;
        head->prev->next = head;
